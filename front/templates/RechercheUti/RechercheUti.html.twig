{% extends 'base.html.twig' %}

{% block title %}Page d'accueil{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
{% endblock %}

{% block body %}
<div class="header">
    <div class="logo">RÉSEAU IIA</div>
    <div class="date">
        {% set jours = ['dimanche', 'lundi', 'mardi', 'mercredi', 'jeudi', 'vendredi', 'samedi'] %}
        {% set mois = ['janvier', 'février', 'mars', 'avril', 'mai', 'juin', 'juillet', 'août', 'septembre', 'octobre', 'novembre', 'décembre'] %}
        {{ jours[date().format('w')]|capitalize }} {{ date().format('d') }} {{ mois[date().format('n') - 1]|capitalize }} {{ date().format('Y') }}
    </div>
    <div class="auth-buttons">
        <a href="{{ path('app_Connexion') }}">
            <button class="btn">Se connecter</button>
        </a>
        <a href="{{ path('app_Inscription') }}">
            <button class="btn">Inscription</button>
        </a>
    </div>
</div>

<div class="example-wrapper">
    <h1>Page de file63</h1>

    <table class="form-table">
        <tr>
            <!-- Colonne gauche : les carrés bleus -->
            <td class="form-column">
                <div class="info-container">
                    <div class="info-box">31/03/2010 10h05 – Je suis en Cours de PHP & MySQL</div>
                </div>
                <div class="info-container">
                    <div class="info-box">15/03/2015 10h05 – Je connais l'apocalypse</div>
                </div>
                <div class="info-container">
                    <div class="info-box">01/01/2020 08h00 – Bonne année à tous !</div>
                </div>
                <div class="info-container">
                    <div class="info-box">15/05/2025 14h22 – Symfony, c’est trop puissant 😎</div>
                </div>
            </td>

            <!-- Colonne droite : la photo -->
            <td class="image-column">
                <div class="profile-photo-box">
                    <img src="{{ asset('uploads/logoaccueil.png') }}" alt="Votre photo" class="profile-photo">
                </div>
            </td>
        </tr>
    </table>
</div>
{% endblock %}
